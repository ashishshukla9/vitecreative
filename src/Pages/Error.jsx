import React from 'react';

const Error = () => {
    return (
        <div>
            Error Page

        </div>
    );
};

export default Error; // Ensure this line is exporting the component as default