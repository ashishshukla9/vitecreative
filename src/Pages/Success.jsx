import React from 'react';

const Success = () => {
    return (
        <div>
            Success Page
            
        </div>
    );
};

export default Success; // Ensure this line is exporting the component as default